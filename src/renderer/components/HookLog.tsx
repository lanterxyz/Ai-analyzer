import React from 'react'

const HookLog: React.FC = () => {
  return (
    <div className="hook-log">
      <p className="empty-hint">JS Hook records will appear here during capture</p>
    </div>
  )
}

export default HookLog
