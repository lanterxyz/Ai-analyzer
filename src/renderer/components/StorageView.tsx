import React from 'react'

const StorageView: React.FC = () => {
  return (
    <div className="storage-view">
      <p className="empty-hint">Storage snapshots will appear here during capture</p>
    </div>
  )
}

export default StorageView
